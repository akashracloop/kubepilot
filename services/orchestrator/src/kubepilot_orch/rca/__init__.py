"""RCA support libraries (Phase 3).

``runtimes/`` holds per-runtime reasoning knowledge (JVM/Node/Python/Go) as
markdown, selected by the Logs agent's ``detail.runtime`` tag and injected into
the RCA prompt — the intelligence is data, not branching code.
"""
