"""KubePilot AI evaluation suite.

Top-level package so the harness is importable as ``eval.harness.*`` from the
repo root (see ``eval/conftest.py`` for the pytest path shim).
"""

from __future__ import annotations
